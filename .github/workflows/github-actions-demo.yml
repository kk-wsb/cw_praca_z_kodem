name: My First Github Workflow
on: [push]
jobs:
  check-versions:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Checking Docker and Python version"
      - name: Check Docker & Python version"
    uses: actions/checkout@v3
  test:
  build:
